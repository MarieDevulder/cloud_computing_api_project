name: build app

env: 
  AZURE_APP_SERVICE_NAME: bondon-grousseau-mallet-plot-python-app-service
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

on:
  push:
    branches:
    - feat/database-and-blob

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@f95db51fddba0c2d1ec667646a06c2ce06100226 
      
      
      - name: Login to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@343f7c4344506bcbf9b4de18042ae17996df046d
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@96383f45573cb7f253c731d3b3ab81c87ef81934
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      
      
      - name: Build and push
        id: build-push
        uses: docker/build-push-action@0565240e2d4ab88bba5387d719585280857ece09
        with:
          context: ./api
          file: ./api/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max


 # Impossible to deploy infrastructure as we don't have access to Azure app registration portal : 
  # Here is the error message when trying to create CLIENT_ID, CLIENT_SECRET : Your administrator has disabled the App registrations experience in the Azure portal.

  # So I will just comment the code below, but it should work if you have access to the Azure app registration portal and then add secrets to Github repo


  ########################################################################################
  # NOTE THAT THIS CODE BELOW HAS BEEN PROVIDED KINDLY BY Emilie Clary AND HER TEAMMATES #
  ########################################################################################


  # deploy_infrastructure:
  #   needs: build_docker_image
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3

  #     - name: Setup Terraform
  #       uses: hashicorp/setup-terraform@v2
  #       with:
  #         terraform_version: 1.5.5

  #     - name: Navigate to infrastructure folder
  #       run: cd infrastructure

  #     - name: Initialize Terraform
  #       run: terraform init

  #     - name: Validate Terraform
  #       run: terraform validate

  #     - name: Apply Terraform
  #       env:
  #         ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  #         ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
  #         ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  #         ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  #       run: terraform apply -auto-approve
